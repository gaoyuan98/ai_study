## Content input and output
- UTF-8 encoding Always use 'chcp 65001' before running shell commands that produce text output on Windows to ensure 
  UTF-8 encoding and avoid garbled characters.
- All content should be replied in Simplified Chinese, including reasoning logic.
- Always Calling Python tools is prohibited